class Application
{
    function run() {
        if(false) {
            puts("This should not be printed");
        } 
        
        if(true) {
            puts("Hellow from an if!")
        }

        return 0;
    }
}