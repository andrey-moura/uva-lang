class Application
{
    function run() {
        if(false) {
            puts("This should not be printed!");
        } 
        
        if(true) {
            puts("Hellow world from if!")
        }

        return 0;
    }
}