class Application
{
    function run()
    {
        var array = ["Hello", "from", "a", "foreach!"];
        
        // You can use 'in', ':' or 'of'
        foreach(var element in array) {
            print(element);
            print(" ");
        }

        puts("");
        
        return 0;
    }
}