class Application
{
    function run()
    {
        var messages = ["Hello", "from", "an", "while", "loop"];

        while(messages.size() > 0)
        {
            print(messages.front());
            messages.pop_front!;

            if(messages.size() > 0)
            {
                print(" ");
            }
        }

        while(true) {
            puts("!");
            break;
        }

        return 0;
    }
}